---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: managedserviceaccountreplicasets.authentication.appthrust.io
spec:
  group: authentication.appthrust.io
  names:
    kind: ManagedServiceAccountReplicaSet
    listKind: ManagedServiceAccountReplicaSetList
    plural: managedserviceaccountreplicasets
    shortNames:
    - msars
    singular: managedserviceaccountreplicaset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: ManagedClusters selected by placementRefs
      jsonPath: .status.selectedClusterCount
      name: Selected
      type: integer
    - description: Selected clusters reconciled with the latest child resources
      jsonPath: .status.readyClusterCount
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ManagedServiceAccountReplicaSet fans out OCM ManagedServiceAccount children
          to ManagedClusters selected by spec.placementRefs.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ManagedServiceAccountReplicaSetSpec declares one OCM managed-serviceaccount
              intent across clusters selected by placementRefs.
            properties:
              placementRefs:
                description: |-
                  PlacementRefs select target ManagedClusters from same-namespace OCM
                  Placement and PlacementDecision output.
                items:
                  description: PlacementRef points to a same-namespace OCM Placement.
                  properties:
                    name:
                      description: Name of the OCM Placement in the same namespace.
                      maxLength: 253
                      minLength: 1
                      type: string
                    rolloutStrategy:
                      default:
                        type: All
                      description: |-
                        RolloutStrategy controls how generated ManagedServiceAccounts and
                        optional RBAC ManifestWorks are applied across clusters selected by this
                        placement ref.
                      properties:
                        all:
                          description: all defines required fields for RolloutStrategy
                            type All
                          properties:
                            maxFailures:
                              anyOf:
                              - type: integer
                              - type: string
                              default: 0
                              description: |-
                                MaxFailures is a percentage or number of clusters in the current rollout that can fail before
                                proceeding to the next rollout. Fail means the cluster has a failed status or timeout status
                                (does not reach successful status after ProgressDeadline).
                                Once the MaxFailures is breached, the rollout will stop.
                                MaxFailures is only considered for rollout types Progressive and ProgressivePerGroup. For
                                Progressive, this is considered over the total number of clusters. For ProgressivePerGroup,
                                this is considered according to the size of the current group. For both Progressive and
                                ProgressivePerGroup, the MaxFailures does not apply for MandatoryDecisionGroups, which tolerate
                                no failures.
                                Default is that no failures are tolerated.
                              pattern: ^((100|[0-9]{1,2})%|[0-9]+)$
                              x-kubernetes-int-or-string: true
                            minSuccessTime:
                              default: "0"
                              description: |-
                                MinSuccessTime is a "soak" time. In other words, the minimum amount of time the workload
                                applier controller will wait from the start of each rollout before proceeding (assuming a
                                successful state has been reached and MaxFailures wasn't breached).
                                MinSuccessTime is only considered for rollout types Progressive and ProgressivePerGroup.
                                The default value is 0 meaning the workload applier proceeds immediately after a successful
                                state is reached.
                                MinSuccessTime must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              type: string
                            progressDeadline:
                              default: None
                              description: |-
                                ProgressDeadline defines how long workload applier controller will wait for the workload to
                                reach a successful state in the cluster.
                                If the workload does not reach a successful state after ProgressDeadline, will stop waiting
                                and workload will be treated as "timeout" and be counted into MaxFailures. Once the MaxFailures
                                is breached, the rollout will stop.
                                ProgressDeadline default value is "None", meaning the workload applier will wait for a
                                successful state indefinitely.
                                ProgressDeadline must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              pattern: ^(([0-9])+[h|m|s])|None$
                              type: string
                          type: object
                        progressive:
                          description: progressive defines required fields for RolloutStrategy
                            type Progressive
                          properties:
                            mandatoryDecisionGroups:
                              description: |-
                                List of the decision groups names or indexes to apply the workload first and fail if workload
                                did not reach successful state.
                                GroupName or GroupIndex must match with the decisionGroups defined in the placement's
                                decisionStrategy
                              items:
                                description: |-
                                  MandatoryDecisionGroup set the decision group name or group index.
                                  GroupName is considered first to select the decisionGroups then GroupIndex.
                                properties:
                                  groupIndex:
                                    description: |-
                                      groupIndex of the decision group should match the placementDecisions label value with label key
                                      cluster.open-cluster-management.io/decision-group-index
                                    format: int32
                                    type: integer
                                  groupName:
                                    description: |-
                                      groupName of the decision group should match the placementDecisions label value with label key
                                      cluster.open-cluster-management.io/decision-group-name
                                    type: string
                                type: object
                              type: array
                            maxConcurrency:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                maxConcurrency is the max number of clusters to deploy workload concurrently. The default value
                                for MaxConcurrency is determined from the clustersPerDecisionGroup defined in the
                                placement->DecisionStrategy.
                              pattern: ^((100|[0-9]{1,2})%|[0-9]+)$
                              x-kubernetes-int-or-string: true
                            maxFailures:
                              anyOf:
                              - type: integer
                              - type: string
                              default: 0
                              description: |-
                                MaxFailures is a percentage or number of clusters in the current rollout that can fail before
                                proceeding to the next rollout. Fail means the cluster has a failed status or timeout status
                                (does not reach successful status after ProgressDeadline).
                                Once the MaxFailures is breached, the rollout will stop.
                                MaxFailures is only considered for rollout types Progressive and ProgressivePerGroup. For
                                Progressive, this is considered over the total number of clusters. For ProgressivePerGroup,
                                this is considered according to the size of the current group. For both Progressive and
                                ProgressivePerGroup, the MaxFailures does not apply for MandatoryDecisionGroups, which tolerate
                                no failures.
                                Default is that no failures are tolerated.
                              pattern: ^((100|[0-9]{1,2})%|[0-9]+)$
                              x-kubernetes-int-or-string: true
                            minSuccessTime:
                              default: "0"
                              description: |-
                                MinSuccessTime is a "soak" time. In other words, the minimum amount of time the workload
                                applier controller will wait from the start of each rollout before proceeding (assuming a
                                successful state has been reached and MaxFailures wasn't breached).
                                MinSuccessTime is only considered for rollout types Progressive and ProgressivePerGroup.
                                The default value is 0 meaning the workload applier proceeds immediately after a successful
                                state is reached.
                                MinSuccessTime must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              type: string
                            progressDeadline:
                              default: None
                              description: |-
                                ProgressDeadline defines how long workload applier controller will wait for the workload to
                                reach a successful state in the cluster.
                                If the workload does not reach a successful state after ProgressDeadline, will stop waiting
                                and workload will be treated as "timeout" and be counted into MaxFailures. Once the MaxFailures
                                is breached, the rollout will stop.
                                ProgressDeadline default value is "None", meaning the workload applier will wait for a
                                successful state indefinitely.
                                ProgressDeadline must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              pattern: ^(([0-9])+[h|m|s])|None$
                              type: string
                          type: object
                        progressivePerGroup:
                          description: progressivePerGroup defines required fields
                            for RolloutStrategy type ProgressivePerGroup
                          properties:
                            mandatoryDecisionGroups:
                              description: |-
                                List of the decision groups names or indexes to apply the workload first and fail if workload
                                did not reach successful state.
                                GroupName or GroupIndex must match with the decisionGroups defined in the placement's
                                decisionStrategy
                              items:
                                description: |-
                                  MandatoryDecisionGroup set the decision group name or group index.
                                  GroupName is considered first to select the decisionGroups then GroupIndex.
                                properties:
                                  groupIndex:
                                    description: |-
                                      groupIndex of the decision group should match the placementDecisions label value with label key
                                      cluster.open-cluster-management.io/decision-group-index
                                    format: int32
                                    type: integer
                                  groupName:
                                    description: |-
                                      groupName of the decision group should match the placementDecisions label value with label key
                                      cluster.open-cluster-management.io/decision-group-name
                                    type: string
                                type: object
                              type: array
                            maxFailures:
                              anyOf:
                              - type: integer
                              - type: string
                              default: 0
                              description: |-
                                MaxFailures is a percentage or number of clusters in the current rollout that can fail before
                                proceeding to the next rollout. Fail means the cluster has a failed status or timeout status
                                (does not reach successful status after ProgressDeadline).
                                Once the MaxFailures is breached, the rollout will stop.
                                MaxFailures is only considered for rollout types Progressive and ProgressivePerGroup. For
                                Progressive, this is considered over the total number of clusters. For ProgressivePerGroup,
                                this is considered according to the size of the current group. For both Progressive and
                                ProgressivePerGroup, the MaxFailures does not apply for MandatoryDecisionGroups, which tolerate
                                no failures.
                                Default is that no failures are tolerated.
                              pattern: ^((100|[0-9]{1,2})%|[0-9]+)$
                              x-kubernetes-int-or-string: true
                            minSuccessTime:
                              default: "0"
                              description: |-
                                MinSuccessTime is a "soak" time. In other words, the minimum amount of time the workload
                                applier controller will wait from the start of each rollout before proceeding (assuming a
                                successful state has been reached and MaxFailures wasn't breached).
                                MinSuccessTime is only considered for rollout types Progressive and ProgressivePerGroup.
                                The default value is 0 meaning the workload applier proceeds immediately after a successful
                                state is reached.
                                MinSuccessTime must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              type: string
                            progressDeadline:
                              default: None
                              description: |-
                                ProgressDeadline defines how long workload applier controller will wait for the workload to
                                reach a successful state in the cluster.
                                If the workload does not reach a successful state after ProgressDeadline, will stop waiting
                                and workload will be treated as "timeout" and be counted into MaxFailures. Once the MaxFailures
                                is breached, the rollout will stop.
                                ProgressDeadline default value is "None", meaning the workload applier will wait for a
                                successful state indefinitely.
                                ProgressDeadline must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              pattern: ^(([0-9])+[h|m|s])|None$
                              type: string
                          type: object
                        type:
                          default: All
                          enum:
                          - All
                          - Progressive
                          - ProgressivePerGroup
                          type: string
                      type: object
                  required:
                  - name
                  type: object
                maxItems: 16
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              rbac:
                description: |-
                  RBAC declares typed remote RBAC to bind to the generated managed service
                  account on every selected managed cluster.
                properties:
                  grants:
                    description: |-
                      Grants render typed remote RBAC resources on each selected managed
                      cluster.
                    items:
                      description: |-
                        RBACGrant renders one closed-domain RBAC grant. Role grants expand to one
                        Role and RoleBinding per target namespace. ClusterRole grants render one
                        ClusterRole and ClusterRoleBinding per selected managed cluster.
                      properties:
                        forEachNamespace:
                          description: ForEachNamespace declares namespace fan-out
                            for Role grants.
                          properties:
                            name:
                              description: Name targets one namespace.
                              maxLength: 63
                              minLength: 1
                              pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                              type: string
                            names:
                              description: Names targets a fixed list of namespaces.
                              items:
                                maxLength: 63
                                minLength: 1
                                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                                type: string
                              minItems: 1
                              type: array
                              x-kubernetes-list-type: set
                            selector:
                              description: |-
                                Selector targets the current remote namespaces whose labels match this
                                Kubernetes label selector.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                        id:
                          description: |-
                            ID is the stable grant identity. Renaming it is treated as deleting one
                            generated grant and creating another.
                          maxLength: 63
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        metadata:
                          description: |-
                            Metadata is copied to generated RBAC resources after controller-reserved
                            keys are rejected by validation.
                          properties:
                            annotations:
                              additionalProperties:
                                type: string
                              description: |-
                                Annotations are copied to the generated RBAC objects after
                                controller-reserved keys are rejected by validation.
                              maxProperties: 32
                              type: object
                            labels:
                              additionalProperties:
                                type: string
                              description: |-
                                Labels are copied to the generated RBAC objects after
                                controller-reserved keys are rejected by validation.
                              maxProperties: 32
                              type: object
                            name:
                              description: |-
                                Name is applied as metadata.name to the generated Role or ClusterRole
                                and to its matching RoleBinding or ClusterRoleBinding on managed
                                clusters.
                              maxLength: 63
                              minLength: 1
                              pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                              type: string
                          required:
                          - name
                          type: object
                        rules:
                          description: Rules are copied to the generated Role or ClusterRole.
                          items:
                            description: |-
                              PolicyRule holds information that describes a policy rule, but does not contain information
                              about who the rule applies to or which namespace the rule applies to.
                            properties:
                              apiGroups:
                                description: |-
                                  APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                                  the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                              nonResourceURLs:
                                description: |-
                                  NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                                  Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                                  Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                              resourceNames:
                                description: ResourceNames is an optional white list
                                  of names that the rule applies to.  An empty set
                                  means that everything is allowed.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                              resources:
                                description: Resources is a list of resources this
                                  rule applies to. '*' represents all resources.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                              verbs:
                                description: Verbs is a list of Verbs that apply to
                                  ALL the ResourceKinds contained in this rule. '*'
                                  represents all verbs.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - verbs
                            type: object
                          maxItems: 32
                          minItems: 1
                          type: array
                          x-kubernetes-list-type: atomic
                        type:
                          description: Type selects the closed generated RBAC resource
                            family.
                          enum:
                          - Role
                          - ClusterRole
                          type: string
                      required:
                      - id
                      - metadata
                      - rules
                      - type
                      type: object
                      x-kubernetes-validations:
                      - message: Role grants require forEachNamespace and ClusterRole
                          grants must omit it
                        rule: 'self.type == ''ClusterRole'' ? !has(self.forEachNamespace)
                          : has(self.forEachNamespace)'
                      - message: forEachNamespace must set exactly one of name, names,
                          or selector
                        rule: '!has(self.forEachNamespace) || ((has(self.forEachNamespace.name)
                          ? 1 : 0) + (has(self.forEachNamespace.names) ? 1 : 0) +
                          (has(self.forEachNamespace.selector) ? 1 : 0)) == 1'
                    maxItems: 128
                    type: array
                    x-kubernetes-list-map-keys:
                    - id
                    x-kubernetes-list-type: map
                type: object
                x-kubernetes-validations:
                - message: spec.rbac.grants[].id must be unique
                  rule: '!has(self.grants) || self.grants.all(g, self.grants.exists_one(h,
                    h.id == g.id))'
              template:
                description: Template is the per-cluster ManagedServiceAccount template.
                properties:
                  metadata:
                    description: Metadata is copied to each generated ManagedServiceAccount.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are copied to each generated ManagedServiceAccount after
                          controller-reserved keys are rejected by validation.
                        maxProperties: 32
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are copied to each generated ManagedServiceAccount after
                          controller-reserved keys are rejected by validation.
                        maxProperties: 32
                        type: object
                      name:
                        description: |-
                          Name is the ServiceAccount name created on each managed cluster and
                          the metadata.name of every generated hub-side ManagedServiceAccount.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      namespace:
                        description: |-
                          Namespace is the ServiceAccount installation namespace on managed
                          clusters. This is not the hub namespace where child ManagedServiceAccount
                          objects are created.
                        maxLength: 63
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  spec:
                    description: Spec is the per-cluster ManagedServiceAccount spec.
                    properties:
                      rotation:
                        description: Rotation mirrors upstream ManagedServiceAccount
                          rotation settings.
                        properties:
                          enabled:
                            default: true
                            description: Enabled prescribes whether token rotation
                              is enabled.
                            type: boolean
                          validity:
                            description: Validity is the requested token validity
                              duration.
                            type: string
                        type: object
                      ttlSecondsAfterCreation:
                        description: TTLSecondsAfterCreation is passed through only
                          when explicitly set.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                required:
                - metadata
                - spec
                type: object
            required:
            - placementRefs
            - template
            type: object
          status:
            description: |-
              ManagedServiceAccountReplicaSetStatus summarizes fan-out state without
              credential material.
            properties:
              conditions:
                description: |-
                  Conditions summarize top-level placement, rollout, readiness, and
                  cleanup state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              controllerAccess:
                description: |-
                  ControllerAccess summarizes the internal namespace-reader bootstrap
                  access used only for selector-targeted grants.
                properties:
                  conditions:
                    description: Conditions summarize controller-access readiness.
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    maxItems: 8
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                  desiredClusterCount:
                    description: |-
                      DesiredClusterCount is the selected cluster count that needs internal
                      namespace-reader access.
                    format: int32
                    minimum: 0
                    type: integer
                  readyClusterCount:
                    description: |-
                      ReadyClusterCount is the count whose controller-access MSA and
                      namespace-reader ManifestWork are ready.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              observedGeneration:
                description: ObservedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                minimum: 0
                type: integer
              placements:
                description: Placements reports per-ref resolution and child aggregation.
                items:
                  description: PlacementStatus is the per-ref view of resolution and
                    child aggregation.
                  properties:
                    availableDecisionGroups:
                      description: |-
                        AvailableDecisionGroups mirrors the OCM ManifestWorkReplicaSet decision
                        group progress message for this placement ref.
                      type: string
                    conditions:
                      description: |-
                        Conditions report per-ref resolution state, such as PlacementResolved
                        and PlacementEmpty.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      maxItems: 8
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    name:
                      description: |-
                        Name identifies the placementRef this status describes and matches the
                        corresponding spec.placementRefs[*].name entry.
                      type: string
                    rollout:
                      description: Rollout summarizes rollout progress for this placement
                        ref.
                      properties:
                        conditions:
                          description: Conditions summarize rollout progression.
                          items:
                            description: Condition contains details for one aspect
                              of the current state of this API Resource.
                            properties:
                              lastTransitionTime:
                                description: |-
                                  lastTransitionTime is the last time the condition transitioned from one status to another.
                                  This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                                format: date-time
                                type: string
                              message:
                                description: |-
                                  message is a human readable message indicating details about the transition.
                                  This may be an empty string.
                                maxLength: 32768
                                type: string
                              observedGeneration:
                                description: |-
                                  observedGeneration represents the .metadata.generation that the condition was set based upon.
                                  For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                  with respect to the current state of the instance.
                                format: int64
                                minimum: 0
                                type: integer
                              reason:
                                description: |-
                                  reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                  Producers of specific condition types may define expected values and meanings for this field,
                                  and whether the values are considered a guaranteed API.
                                  The value should be a CamelCase string.
                                  This field may not be empty.
                                maxLength: 1024
                                minLength: 1
                                pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                                type: string
                              status:
                                description: status of the condition, one of True,
                                  False, Unknown.
                                enum:
                                - "True"
                                - "False"
                                - Unknown
                                type: string
                              type:
                                description: type of condition in CamelCase or in
                                  foo.example.com/CamelCase.
                                maxLength: 316
                                pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                                type: string
                            required:
                            - lastTransitionTime
                            - message
                            - reason
                            - status
                            - type
                            type: object
                          maxItems: 4
                          type: array
                          x-kubernetes-list-map-keys:
                          - type
                          x-kubernetes-list-type: map
                        failed:
                          description: |-
                            Failed is the number of selected ManagedClusters in Failed state outside
                            the active rollout window.
                          format: int32
                          minimum: 0
                          type: integer
                        succeeded:
                          description: |-
                            Succeeded is the number of selected ManagedClusters that completed
                            rollout and are no longer in the active rollout window.
                          format: int32
                          minimum: 0
                          type: integer
                        timedOut:
                          description: |-
                            TimedOut is the number of selected ManagedClusters whose rollout exceeded
                            progressDeadline.
                          format: int32
                          minimum: 0
                          type: integer
                        total:
                          description: |-
                            Total is the number of selected ManagedClusters considered by this
                            rollout summary.
                          format: int32
                          minimum: 0
                          type: integer
                        updating:
                          description: |-
                            Updating is the number of selected ManagedClusters currently in the
                            rollout window, including ToApply, Progressing, Failed within deadline,
                            and Succeeded within minSuccessTime.
                          format: int32
                          minimum: 0
                          type: integer
                      type: object
                    selectedClusterCount:
                      description: |-
                        SelectedClusterCount is the number of ManagedClusters resolved from
                        this placement ref.
                      format: int32
                      minimum: 0
                      type: integer
                    summary:
                      description: |-
                        Summary aggregates child RBAC ManifestWork state scoped to the
                        ManagedClusters selected by this placement ref.
                      properties:
                        applied:
                          description: |-
                            Applied is the number of observed RBAC ManifestWork objects whose
                            Applied condition is True for the current generation.
                          format: int32
                          minimum: 0
                          type: integer
                        available:
                          description: |-
                            Available is the number of observed RBAC ManifestWork objects whose
                            Available condition is True for the current generation.
                          format: int32
                          minimum: 0
                          type: integer
                        degraded:
                          description: |-
                            Degraded is the number of observed RBAC ManifestWork objects whose
                            Degraded condition is True for the current generation.
                          format: int32
                          minimum: 0
                          type: integer
                        desiredTotal:
                          description: |-
                            DesiredTotal is the number of RBAC ManifestWork objects expected across
                            all selected ManagedClusters for the current spec.
                          format: int32
                          minimum: 0
                          type: integer
                        progressing:
                          description: |-
                            Progressing is the number of observed RBAC ManifestWork objects whose
                            Progressing condition is True for the current generation.
                          format: int32
                          minimum: 0
                          type: integer
                        total:
                          description: |-
                            Total is the number of RBAC ManifestWork objects observed across all
                            selected ManagedClusters.
                          format: int32
                          minimum: 0
                          type: integer
                        updated:
                          description: |-
                            Updated is the number of observed RBAC ManifestWork objects whose spec
                            hash matches the desired manifests hash and are therefore not stale.
                          format: int32
                          minimum: 0
                          type: integer
                      type: object
                  required:
                  - name
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              readyClusterCount:
                description: |-
                  ReadyClusterCount is the number of selected clusters whose child MSA is
                  reconciled and whose RBAC ManifestWork, when present, is Available.
                format: int32
                minimum: 0
                type: integer
              rollout:
                description: |-
                  Rollout summarizes rollout progress across the union of selected
                  ManagedClusters.
                properties:
                  conditions:
                    description: Conditions summarize rollout progression.
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    maxItems: 4
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                  failed:
                    description: |-
                      Failed is the number of selected ManagedClusters in Failed state outside
                      the active rollout window.
                    format: int32
                    minimum: 0
                    type: integer
                  succeeded:
                    description: |-
                      Succeeded is the number of selected ManagedClusters that completed
                      rollout and are no longer in the active rollout window.
                    format: int32
                    minimum: 0
                    type: integer
                  timedOut:
                    description: |-
                      TimedOut is the number of selected ManagedClusters whose rollout exceeded
                      progressDeadline.
                    format: int32
                    minimum: 0
                    type: integer
                  total:
                    description: |-
                      Total is the number of selected ManagedClusters considered by this
                      rollout summary.
                    format: int32
                    minimum: 0
                    type: integer
                  updating:
                    description: |-
                      Updating is the number of selected ManagedClusters currently in the
                      rollout window, including ToApply, Progressing, Failed within deadline,
                      and Succeeded within minSuccessTime.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              selectedClusterCount:
                description: |-
                  SelectedClusterCount is the union-deduplicated number of ManagedClusters
                  resolved from spec.placementRefs.
                format: int32
                minimum: 0
                type: integer
              summary:
                description: Summary aggregates child RBAC ManifestWork state.
                properties:
                  applied:
                    description: |-
                      Applied is the number of observed RBAC ManifestWork objects whose
                      Applied condition is True for the current generation.
                    format: int32
                    minimum: 0
                    type: integer
                  available:
                    description: |-
                      Available is the number of observed RBAC ManifestWork objects whose
                      Available condition is True for the current generation.
                    format: int32
                    minimum: 0
                    type: integer
                  degraded:
                    description: |-
                      Degraded is the number of observed RBAC ManifestWork objects whose
                      Degraded condition is True for the current generation.
                    format: int32
                    minimum: 0
                    type: integer
                  desiredTotal:
                    description: |-
                      DesiredTotal is the number of RBAC ManifestWork objects expected across
                      all selected ManagedClusters for the current spec.
                    format: int32
                    minimum: 0
                    type: integer
                  progressing:
                    description: |-
                      Progressing is the number of observed RBAC ManifestWork objects whose
                      Progressing condition is True for the current generation.
                    format: int32
                    minimum: 0
                    type: integer
                  total:
                    description: |-
                      Total is the number of RBAC ManifestWork objects observed across all
                      selected ManagedClusters.
                    format: int32
                    minimum: 0
                    type: integer
                  updated:
                    description: |-
                      Updated is the number of observed RBAC ManifestWork objects whose spec
                      hash matches the desired manifests hash and are therefore not stale.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.template.metadata.labels must not contain controller-reserved
            keys
          rule: '!has(self.spec.template.metadata.labels) || self.spec.template.metadata.labels.all(key,
            !(key in [''app.kubernetes.io/managed-by'', ''app.kubernetes.io/part-of'',
            ''authentication.appthrust.io/set-name'', ''authentication.appthrust.io/set-namespace'',
            ''authentication.appthrust.io/set-uid'', ''authentication.appthrust.io/placement-ref-name'']))'
        - message: spec.template.metadata.annotations must not contain controller-reserved
            keys
          rule: '!has(self.spec.template.metadata.annotations) || self.spec.template.metadata.annotations.all(key,
            !(key in [''app.kubernetes.io/managed-by'', ''app.kubernetes.io/part-of'',
            ''authentication.appthrust.io/set-name'', ''authentication.appthrust.io/set-namespace'',
            ''authentication.appthrust.io/set-uid'', ''authentication.appthrust.io/placement-ref-name'']))'
        - message: spec.rbac.grants[].metadata.labels must not contain controller-reserved
            keys
          rule: '!has(self.spec.rbac) || !has(self.spec.rbac.grants) || self.spec.rbac.grants.all(g,
            !has(g.metadata.labels) || g.metadata.labels.all(key, !(key in [''app.kubernetes.io/managed-by'',
            ''app.kubernetes.io/part-of'', ''authentication.appthrust.io/set-name'',
            ''authentication.appthrust.io/set-namespace'', ''authentication.appthrust.io/set-uid'',
            ''authentication.appthrust.io/placement-ref-name'', ''authentication.appthrust.io/grant-id'',
            ''authentication.appthrust.io/slice-type'', ''authentication.appthrust.io/target-namespace'',
            ''authentication.appthrust.io/target-namespace-hash'', ''authentication.appthrust.io/spec-hash'',
            ''authentication.open-cluster-management.io/sync-to-clusterprofile''])))'
        - message: spec.rbac.grants[].metadata.annotations must not contain controller-reserved
            keys
          rule: '!has(self.spec.rbac) || !has(self.spec.rbac.grants) || self.spec.rbac.grants.all(g,
            !has(g.metadata.annotations) || g.metadata.annotations.all(key, !(key
            in [''app.kubernetes.io/managed-by'', ''app.kubernetes.io/part-of'', ''authentication.appthrust.io/set-name'',
            ''authentication.appthrust.io/set-namespace'', ''authentication.appthrust.io/set-uid'',
            ''authentication.appthrust.io/placement-ref-name'', ''authentication.appthrust.io/grant-id'',
            ''authentication.appthrust.io/slice-type'', ''authentication.appthrust.io/target-namespace'',
            ''authentication.appthrust.io/target-namespace-hash'', ''authentication.appthrust.io/spec-hash'',
            ''authentication.open-cluster-management.io/sync-to-clusterprofile''])))'
    served: true
    storage: true
    subresources:
      status: {}
