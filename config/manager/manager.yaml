apiVersion: apps/v1
kind: Deployment
metadata:
  name: ocm-managed-serviceaccount-set-operator
  labels:
    app.kubernetes.io/name: ocm-managed-serviceaccount-set-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ocm-managed-serviceaccount-set-operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ocm-managed-serviceaccount-set-operator
    spec:
      serviceAccountName: ocm-managed-serviceaccount-set-operator
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          image: ghcr.io/appthrust/ocm-managed-serviceaccount-set-operator:dev
          imagePullPolicy: IfNotPresent
          args:
            - --metrics-bind-address=:8080
            - --health-probe-bind-address=:8081
            - --leader-elect
          ports:
            - name: metrics
              containerPort: 8080
            - name: health
              containerPort: 8081
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 128Mi

